<Window x:Class="KafeProject.View.All_Windows.MenuCheck"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KafeProject.View.All_Windows"
        mc:Ignorable="d"
        Height="Auto" MaxHeight="800" Width="500" Background="{x:Null}"
        AllowsTransparency="True" WindowStyle="None" 
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Topmost="True">
    <Grid>
        <Grid Margin="15,15,15,15" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="90"/>
            </Grid.RowDefinitions>
            <Grid.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.5" ShadowDepth="7" Direction="272"/>
            </Grid.Effect>
            <ListBox x:Name="Check_List" Width="450" Margin="0,15,0,0" VerticalAlignment="Stretch" HorizontalAlignment="Center">
                <Button Height="50" Width="450"/>
            </ListBox>
            <Button  Style="{DynamicResource Button_Yellow}" Grid.Row="1" Margin="0,10,0,0" Height="60"  Width="450" VerticalAlignment="Top" HorizontalAlignment="Center" Content="Создать новый чек" FontSize="22"/>
        </Grid>
    </Grid>
</Window>
